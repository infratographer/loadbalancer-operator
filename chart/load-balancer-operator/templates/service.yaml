apiVersion: v1
kind: Service
metadata:
  name: {{ include "load-balancer-operator.fullname" . }}
  labels:
    {{- include "load-balancer-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "load-balancer-operator.selectorLabels" . | nindent 4 }}
